#include <iostream>
using namespace std;

int main()
{
    unsigned int a = 30, b = 20;
    unsigned int diff = a - b; // we cant go into negatives with diff
    cout << "diff is " << diff << endl;
    return 0;
}
